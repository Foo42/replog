export { ClockedLog, ClockedValue, ConflictResolutionFunc, dropConflicts, mergeLogs } from './clockedLog'
export { Clock, compareClocks, currentTime, mergeClocks, incrementKey, ClockComparisonResult } from './vectorClock'
